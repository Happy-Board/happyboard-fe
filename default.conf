server {
    listen       80;
    listen  [::]:80;
    server_name  happyboard.io.vn;

    # Enable access log for monitoring
    # access_log  /var/log/nginx/host.access.log  main;

    location / {
        proxy_pass http://localhost:8888;  # Forward requests to happyboard.io.vn:8888
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Custom error pages (optional)
    # error_page  404              /404.html;

    # Redirect server error pages to the static page /50x.html
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;  # Ensure 50x.html exists in this directory
    }

    # Example of proxying PHP scripts to Apache (commented out)
    # location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    # }

    # Example of passing PHP scripts to FastCGI server (commented out)
    # location ~ \.php$ {
    #    root           html;
    #    fastcgi_pass   127.0.0.1:9000;
    #    fastcgi_index  index.php;
    #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
    #    include        fastcgi_params;
    # }

    # Deny access to .htaccess files for security
    # location ~ /\.ht {
    #    deny  all;
    # }
}
