name: Deploy Vue Application

on:
  push:
    branches:
      - main
      - dev
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      # - name: Create .env file
      #   run: |
      #     echo "VITE_SERVER_URL=${{secrets.VITE_SERVER_URL}}" >> .env
      #     echo "VITE_CLIENT_HOST=${{secrets.VITE_CLIENT_HOST}}" >> .env
      #     echo "VITE_CLIENT_PORT=${{secrets.VITE_CLIENT_PORT}}" >> .env
      #     echo "VITE_FIREBASE_APIKEY=${{secrets.VITE_FIREBASE_APIKEY}}" >> .env
      #     echo "VITE_FIREBASE_AUTH_DOMAIN=${{secrets.VITE_FIREBASE_AUTH_DOMAIN}}" >> .env
      #     echo "VITE_FIREBASE_PROJECT_ID=${{secrets.VITE_FIREBASE_PROJECT_ID}}" >> .env
      #     echo "VITE_FIREBASE_STORAGE_BUCKET=${{secrets.VITE_FIREBASE_STORAGE_BUCKET}}" >> .env
      #     echo "VITE_FIREBASE_MESSAGING_SENDER_ID=${{secrets.VITE_FIREBASE_MESSAGING_SENDER_ID}}" >> .env
      #     echo "VITE_FIREBASE_APP_ID=${{secrets.VITE_FIREBASE_APP_ID}}" >> .env
      #     echo "VITE_FIREBASE_MEASUREMRNT_ID=${{secrets.VITE_FIREBASE_MEASUREMRNT_ID}}" >> .env

      - name: Login to docker hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PW }}
      - name: Build docker image
        run: docker build -t duymt3/happyboard-fe .
      - name: Publish image to docker hub
        run: docker push duymt3/happyboard-fe:latest

  # deploy:
  #   needs: build
  #   runs-on: self-hosted
  #   steps:
  #     - name: Stop old container
  #       run: docker stop happyboard-fe-container
  #     - name: Remove old container
  #       run: docker rm happyboard-fe-container
  #     - name: Remove old image
  #       run: docker rmi duymt3/happyboard-fe:latest
  #     - name: Pull image from docker hub
  #       run: docker pull duymt3/happyboard-fe:latest
  #     - name: Run docker container
  #       run: docker run -d -it -p 8888:8888 --name happyboard-fe-container duymt3/happyboard-fe:latest
